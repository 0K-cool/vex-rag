{
  "name": "vex-rag",
  "version": "1.2.0",
  "description": "100% local RAG system - Python library + MCP server + CLI tools + slash commands",
  "author": "Kelvin Lomboy",
  "repository": "https://github.com/0K-cool/vex-rag",
  "license": "MIT",

  "compatibleWith": {
    "claudeCode": ">=1.0.0"
  },

  "mcpServers": [
    {
      "name": "vex-knowledge-base",
      "type": "stdio",
      "command": "python3",
      "args": ["-m", "mcp_server.vex_kb_server"],
      "env": {
        "RAG_CONFIG": "{project}/.vex-rag.yml",
        "PYTHONPATH": "{plugin_dir}"
      },
      "description": "Automatic context injection from local knowledge base"
    }
  ],

  "commands": [
    {
      "name": "rag-search",
      "file": "commands/search.md",
      "description": "Search vex-rag knowledge base manually"
    },
    {
      "name": "rag-index",
      "file": "commands/index.md",
      "description": "Index a document into vex-rag knowledge base"
    }
  ],

  "dependencies": {
    "python": ">=3.11",
    "ollama": ">=0.1.0"
  },

  "configuration": {
    "required": [
      ".vex-rag.yml"
    ],
    "optional": [
      "lance_vex_kb/"
    ]
  },

  "features": {
    "mcpServer": true,
    "cliTools": true,
    "pythonLibrary": true,
    "slashCommands": true,
    "hooks": false,
    "agents": false
  },

  "notes": [
    "CLI tools (vex-search, vex-index) installed via pip",
    "Slash commands (/rag-search, /rag-index) available in conversations",
    "MCP server requires manual configuration in project .mcp.json",
    "See PLUGIN-STATUS.md for roadmap and planned features"
  ]
}
